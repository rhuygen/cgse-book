# Chapter 20 The Process Manager

*Starting, stopping, and keeping track of the other services.*

Modules: `egse/procman/__init__.py`, `egse/procman/procman_cs.py`, `egse/procman/procman_protocol.py`, `egse/procman/procman_ui.py` (all in `cgse-core`)

## 1. What the Process Manager Controls

The Process Manager Control Server's role in handling the Common-EGSE processes, and its relationship to `process.py` (Chapter 12).

TBW.

## 2. Protocol and Control Server

`procman_cs.py` and `procman_protocol.py` as a `ControlServer`/`CommandProtocol` pair (Chapters 6 and 7).

TBW.

## 3. The Process Manager UI

`procman_ui.py` and how an operator interacts with the running set of processes.

TBW.
